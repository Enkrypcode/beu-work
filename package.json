{
  "name": "beu-work",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "generate:pwa-icons": "node scripts/generate-pwa-icons.mjs",
    "prepare:profile-avatars": "node scripts/prepare-profile-avatars.mjs",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.1"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "playwright": "^1.63.0",
    "vite": "^7.1.0"
  }
}
